services:
  - type: web
    name: pons-auto-dashboard
    env: python
    buildCommand: cd Downloads/pons-auto && pip install -r requirements.txt
    startCommand: cd Downloads/pons-auto && streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # Set this manually in Render dashboard
      - key: PYTHON_VERSION
        value: 3.9.18
